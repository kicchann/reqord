{
  "name": "reqord",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/cli && npm run build --workspace=packages/web",
    "clean": "rm -rf packages/*/dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
